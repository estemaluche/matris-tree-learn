use crate::algorithm::{matris::RMatris, probability::Probability};

pub struct Raveai{
    probability:Probability,
}
impl Raveai {
    pub fn new() -> Probability{
        Probability::new()
    }
    pub fn calc_probability<T>(matris:&RMatris<T>,){

    }
}